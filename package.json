{
  "name": "wasm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "asBuild:debug": "asc src/assemblyscript/mandelbrot.ts -b scripts/mandelbrot-debug.wasm -t bin/mandelbrot-debug.wat --sourceMap --validate --debug",
    "asBuild:release": "asc src/assemblyscript/mandelbrot.ts -b scripts/render/wasm-assemblyscript.wasm --validate --optimize",
    "asBuild": "npm run asBuild:release",
    "wabtBuild": "node build/wat2wasm.js",
    "build": "npm run asbuild && npm run wabtBuild",
    "server": "http-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assemblyscript": "github:AssemblyScript/assemblyscript",
    "http-server": "^0.11.1",
    "prettier": "1.18.2",
    "wabt": "^1.0.11"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "none",
    "printWidth": 100
  }
}
